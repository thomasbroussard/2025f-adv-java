# Embedded web server port for the quiz-rest application.
# Change the value below, or override it at runtime without editing this file:
#   java -jar quiz-rest.jar --server.port=9090
#   SERVER_PORT=9090 java -jar quiz-rest.jar
#   mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=9090
server.port=8080
